## GOENV - a package for parsing and loading .env files to the runtime environment variables
* Specify path to .env files using FetchFiles() - you can call it many times before parsing
* Finally, parse all the files and load them to environment with a call to ParseFile() 
